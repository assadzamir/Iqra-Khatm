/**
 * src/app/(tabs)/_layout.tsx
 *
 * WIRING PLACEHOLDER — Group Khatm Integration
 *
 * When the Expo app is initialized (via `npx expo install` / `npx expo prebuild`),
 * integrate the KhatmStackNavigator here.
 *
 * Example integration with Expo Router tabs:
 *
 * import { Tabs } from 'expo-router';
 * import { KhatmStackNavigator } from '@/features/khatm';
 *
 * export default function TabsLayout() {
 *   return (
 *     <Tabs>
 *       <Tabs.Screen name="khatm" options={{ title: 'Khatm', tabBarIcon: ... }} />
 *       // ... other tabs
 *     </Tabs>
 *   );
 * }
 *
 * And create src/app/(tabs)/khatm.tsx:
 *   import { KhatmStackNavigator } from '@/features/khatm';
 *   export default KhatmStackNavigator;
 *
 * Deep link configuration (add to app.json linking config):
 *   "iqra://khatm/join/:joinCode" → GroupKhatm screen with joinCode param
 *
 * Note: KhatmStackNavigator uses @react-navigation/native-stack.
 * Ensure NavigationContainer wraps the app at the root level.
 */

// This file is a placeholder — replace with actual Expo Router layout
// once the app scaffold exists.
export {};
